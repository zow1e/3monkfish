# @petcare/db

ORM-agnostic database scaffolding.

## Notes
- `src/schema/*` stores table-level model placeholders.
- `migrations/` and `seeds/` are intentionally empty placeholders.
- TODO: add pgvector extension migration and embedding index strategy.
